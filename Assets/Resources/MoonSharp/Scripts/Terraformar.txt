return{
	title = "Terraformar",
	type = 'f',
	aspectos = {0, 0, 0, 0},
	desc = "Remove um terreno à sua escolha",
	flavor = "Com persistência, concentração, e um pequeno meteoro, quase qualquer terreno pode perder suas características mais significativas.",
	cost = 2,

	abilities = {
		
		removeTerrain = function(board)
			local pos = board.requestSelectPosition(enemyPosition)
			if pos then
				board.removeTerrain(pos)
			end
		end
		}
	
	}